<UserControl x:Class="MkvToolnixAutomatisierung.Views.BatchMuxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,12"
                   FontSize="15"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap">Batch-Verarbeitung<LineBreak />Ordner wählen, automatisch scannen und passende Episoden gesammelt verarbeiten. Quellen- und TVDB-Prüfungen werden komplett vor dem ersten Mux erledigt.</TextBlock>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="190" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Center" Text="Quellordner" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,4,12,4" IsReadOnly="True" Text="{Binding SourceDirectory, Mode=OneWay}" />
            <Button Grid.Row="0" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding SelectSourceDirectoryCommand}" Content="Ordner wählen" />

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Center" Text="Serienablage" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4,12,4" IsReadOnly="True" Text="{Binding OutputDirectory, Mode=OneWay}" />
            <Button Grid.Row="1" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding SelectOutputDirectoryCommand}" Content="Ablage ändern" />

            <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,8,0,0" TextWrapping="Wrap" Text="Nach der Quellordner-Auswahl startet der Scan direkt. Unter der Serienablage werden die eigentlichen Serien- und Staffelordner automatisch verwendet; bereits vorhandene Zieldateien werden markiert." />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,12">
                <Border Margin="0,0,8,0" Padding="10" Background="#F4F1E6" BorderBrush="#C9BE93" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock FontSize="12" Opacity="0.75" Text="Gefunden" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding EpisodeCount, Mode=OneWay}" />
                    </StackPanel>
                </Border>
                <Border Margin="0,0,8,0" Padding="10" Background="#E8F3FF" BorderBrush="#8CB4D8" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock FontSize="12" Opacity="0.75" Text="Ausgewählt" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding SelectedEpisodeCount, Mode=OneWay}" />
                    </StackPanel>
                </Border>
                <Border Margin="0,0,8,0" Padding="10" Background="#EEF6E8" BorderBrush="#88B06E" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock FontSize="12" Opacity="0.75" Text="Schon in Ablage" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding ExistingArchiveCount, Mode=OneWay}" />
                    </StackPanel>
                </Border>
                <Border Padding="10" Background="#FFF4D6" BorderBrush="#D8B46A" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock FontSize="12" Opacity="0.75" Text="Offene Prüfungen" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding PendingCheckCount, Mode=OneWay}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <GroupBox Grid.Row="1" Header="Gefundene Episoden" MinHeight="280">
                <DataGrid ItemsSource="{Binding EpisodeItems}"
                          SelectedItem="{Binding SelectedEpisodeItem, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          MinHeight="260"
                          Margin="8">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Auswahl" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                        <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="S/E" Binding="{Binding EpisodeCodeDisplayText}" Width="*" IsReadOnly="True" />
                        <DataGridTemplateColumn Header="In Ablage" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Margin="2"
                                            Padding="6,2"
                                            CornerRadius="9"
                                            BorderThickness="1"
                                            BorderBrush="#8CB4D8"
                                            Background="#E8F3FF">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F3FF" />
                                                <Setter Property="BorderBrush" Value="#8CB4D8" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ArchiveStateText}" Value="vorhanden">
                                                        <Setter Property="Background" Value="#EEF6E8" />
                                                        <Setter Property="BorderBrush" Value="#88B06E" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding ArchiveStateText}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Prüfung" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Margin="2"
                                            Padding="6,2"
                                            CornerRadius="9"
                                            BorderThickness="1"
                                            BorderBrush="#C7D1DC"
                                            Background="#F3F6FA">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F3F6FA" />
                                                <Setter Property="BorderBrush" Value="#C7D1DC" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ReviewHint}" Value="Freigegeben">
                                                        <Setter Property="Background" Value="#EEF6E8" />
                                                        <Setter Property="BorderBrush" Value="#88B06E" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ReviewHint}" Value="Keine">
                                                        <Setter Property="Background" Value="#F3F6FA" />
                                                        <Setter Property="BorderBrush" Value="#C7D1DC" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ReviewHint}" Value="Quelle + TVDB prüfen">
                                                        <Setter Property="Background" Value="#FFF4D6" />
                                                        <Setter Property="BorderBrush" Value="#D8B46A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ReviewHint}" Value="Quelle prüfen">
                                                        <Setter Property="Background" Value="#FFF4D6" />
                                                        <Setter Property="BorderBrush" Value="#D8B46A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ReviewHint}" Value="TVDB prüfen">
                                                        <Setter Property="Background" Value="#FFF4D6" />
                                                        <Setter Property="BorderBrush" Value="#D8B46A" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding ReviewHint}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Status" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Margin="2"
                                            Padding="6,2"
                                            CornerRadius="9"
                                            BorderThickness="1"
                                            BorderBrush="#C7D1DC"
                                            Background="#F3F6FA">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F3F6FA" />
                                                <Setter Property="BorderBrush" Value="#C7D1DC" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Bereit">
                                                        <Setter Property="Background" Value="#E8F3FF" />
                                                        <Setter Property="BorderBrush" Value="#8CB4D8" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="In Ablage vorhanden">
                                                        <Setter Property="Background" Value="#EEF6E8" />
                                                        <Setter Property="BorderBrush" Value="#88B06E" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Ziel aktuell">
                                                        <Setter Property="Background" Value="#EEF6E8" />
                                                        <Setter Property="BorderBrush" Value="#88B06E" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Läuft">
                                                        <Setter Property="Background" Value="#FFF4D6" />
                                                        <Setter Property="BorderBrush" Value="#D8B46A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Warnung">
                                                        <Setter Property="Background" Value="#FFF4D6" />
                                                        <Setter Property="BorderBrush" Value="#D8B46A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Fehler">
                                                        <Setter Property="Background" Value="#FCE8E8" />
                                                        <Setter Property="BorderBrush" Value="#D28A8A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Erfolgreich">
                                                        <Setter Property="Background" Value="#EEF6E8" />
                                                        <Setter Property="BorderBrush" Value="#88B06E" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding Status}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Quelle" Binding="{Binding MainVideoFileName}" Width="2*" IsReadOnly="True" SortMemberPath="MainVideoFileName" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <TextBlock Grid.Row="2" Margin="0,12,0,6" Text="{Binding StatusText, Mode=OneWay}" />
            <ProgressBar Grid.Row="3" Height="18" Minimum="0" Maximum="100" Value="{Binding ProgressValue, Mode=OneWay}" />

            <Expander Grid.Row="4"
                      Margin="0,12,0,0"
                      Header="Ausgewählten Eintrag prüfen und anpassen"
                      MaxHeight="320"
                      IsExpanded="False">
                <Border Padding="8"
                        BorderBrush="#D0D0D0"
                        BorderThickness="1"
                        CornerRadius="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  MaxHeight="300">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="170" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Center" Text="Dateititel" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,4,12,4" Text="{Binding SelectedEpisodeItem.TitleForMux, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Row="0" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding RedetectSelectedEpisodeCommand}" Content="Video neu erkennen" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="TVDB-Status" />
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBox Margin="0,4,12,4" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.MetadataDisplayText, Mode=OneWay}" />
                        <TextBox Margin="0,4,12,4" Height="52" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.MetadataStatusText, Mode=OneWay}" />
                    </StackPanel>
                    <Button Grid.Row="1" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding ReviewSelectedMetadataCommand}" Content="TVDB prüfen" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Gefundene Quellen" />
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,4,12,4" Height="64" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.RequestedSourcesDisplayText, Mode=OneWay}" />
                    <Button Grid.Row="2" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding OpenSelectedSourcesCommand}" Content="Quelle prüfen" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Aktive Hauptquelle" />
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,4,12,4" Height="48" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.MainVideoDisplayText, Mode=OneWay}" />
                    <Button Grid.Row="3" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding EditSelectedAudioDescriptionCommand}" Content="AD korrigieren" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Weitere Videos und AD" />
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="0,4,12,4" Height="72" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.VideoAndAudioDescriptionDisplayText, Mode=OneWay}" />
                    <Button Grid.Row="4" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding EditSelectedSubtitlesCommand}" Content="Untertitel korrigieren" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Untertitel" />
                    <TextBox Grid.Row="5" Grid.Column="1" Margin="0,4,12,4" Height="72" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.SubtitleDisplayText, Mode=OneWay}" />
                    <Button Grid.Row="5" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding EditSelectedAttachmentsCommand}" Content="Anhänge korrigieren" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Anhänge / Ausgabe" />
                    <StackPanel Grid.Row="6" Grid.Column="1">
                        <TextBox Margin="0,4,12,4" Height="64" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.AttachmentDisplayText, Mode=OneWay}" />
                        <TextBox Margin="0,4,12,4" IsReadOnly="True" Text="{Binding SelectedEpisodeItem.OutputPath, Mode=OneWay}" />
                    </StackPanel>
                    <Button Grid.Row="6" Grid.Column="2" Margin="0,2,0,2" Padding="10,6" Command="{Binding EditSelectedOutputCommand}" Content="Ausgabe ändern" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Geplante Verwendung" />
                    <Border Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,4,0,4" Padding="10" Background="#F6F4EA" BorderBrush="#B8AA72" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,2,8,2" FontWeight="SemiBold" Text="Serienablage" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,2,0,2" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.ArchiveAction, Mode=OneWay}" />
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.ArchiveDetails, Mode=OneWay}" />
                                    </StackPanel>
                                </TextBlock.ToolTip>
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.ArchiveAction, Mode=OneWay}" />
                                <Run Text=": " />
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.ArchiveDetails, Mode=OneWay}" />
                            </TextBlock>

                            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,2,8,2" FontWeight="SemiBold" Text="Video" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,2,0,2" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.MainVideo, Mode=OneWay}" />
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.AdditionalVideos, Mode=OneWay}" />
                                    </StackPanel>
                                </TextBlock.ToolTip>
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.MainVideo, Mode=OneWay}" />
                                <Run Text=" | weitere: " />
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.AdditionalVideos, Mode=OneWay}" />
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,2,8,2" FontWeight="SemiBold" Text="Audio" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,2,0,2" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.Audio, Mode=OneWay}" />
                                        <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.AudioDescription, Mode=OneWay}" />
                                    </StackPanel>
                                </TextBlock.ToolTip>
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.Audio, Mode=OneWay}" />
                                <Run Text=" | AD: " />
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.AudioDescription, Mode=OneWay}" />
                            </TextBlock>

                            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,2,8,2" FontWeight="SemiBold" Text="Untertitel" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,2,0,2" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.Subtitles, Mode=OneWay}" />
                                </TextBlock.ToolTip>
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.Subtitles, Mode=OneWay}" />
                            </TextBlock>

                            <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,2,8,2" FontWeight="SemiBold" Text="Anhänge" />
                            <TextBlock Grid.Row="4" Grid.Column="1" Margin="0,2,0,2" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <TextBlock Text="{Binding SelectedEpisodeItem.UsageSummary.Attachments, Mode=OneWay}" />
                                </TextBlock.ToolTip>
                                <Run Text="{Binding SelectedEpisodeItem.UsageSummary.Attachments, Mode=OneWay}" />
                            </TextBlock>
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="8" Grid.Column="0" Margin="0,8,12,8" VerticalAlignment="Top" Text="Hinweise" />
                    <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,4,0,4" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" TextWrapping="Wrap" Text="{Binding SelectedEpisodeItem.NotesDisplayText, Mode=OneWay}" />
                </Grid>
                    </ScrollViewer>
                </Border>
            </Expander>

            <Expander Grid.Row="5" Header="Batch-Protokoll" Margin="0,12,0,0" IsExpanded="False">
                <TextBox Margin="8"
                         FontFamily="Consolas"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="140"
                         Text="{Binding LogText, Mode=OneWay}" />
            </Expander>
        </Grid>

        <WrapPanel Grid.Row="3" Margin="0,12,0,0">
            <Button Width="150" Margin="0,0,12,0" Padding="10,6" Command="{Binding ScanDirectoryCommand}" Content="Ordner scannen" />
            <Button Width="130" Margin="0,0,12,0" Padding="10,6" Command="{Binding SelectAllEpisodesCommand}" Content="Alle wählen" />
            <Button Width="140" Margin="0,0,12,0" Padding="10,6" Command="{Binding DeselectAllEpisodesCommand}" Content="Alle abwählen" />
            <Button Width="180" Margin="0,0,12,0" Padding="10,6" Command="{Binding ReviewPendingSourcesCommand}" Content="Pflichtchecks starten" />
            <Button Width="170" Margin="0,0,12,0" Padding="10,6" Command="{Binding RunBatchCommand}" Content="Batch starten" />
        </WrapPanel>
    </Grid>
</UserControl>
